project SDL_TTF is
    for Externally_Built use "true";
    for Library_Dir use "/usr/lib";
    for Library_Name use "SDL2_ttf";
    for Source_Dirs use ();
    for Library_Kind use "dynamic";
end SDL_TTF;
